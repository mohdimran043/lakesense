{
  "permissions": {
    "allow": [
      "Bash(mkdir -p reference backend ml frontend website deploy docs/analysis)",
      "Bash(git init *)",
      "Bash(git clone *)",
      "Bash(git add *)",
      "Bash(git commit -q -m 'Phase 0: workspace setup — structure, gitignore, reference clones, PROGRESS.md *)",
      "Bash(git commit -q -m 'Phase 1: reference analysis of OLake engine + UI; integration strategy chosen \\(UI API polling\\) *)",
      "Bash(git commit -q -m 'Phase 2: product spec with acceptance criteria; ML + correlation brainstorms decided *)",
      "Bash(npm --version)",
      "Bash(docker --version)",
      "Bash(docker compose *)",
      "Bash(git -C /home/imran/Documents/github/LakeSense status --short)",
      "Bash(git -C /home/imran/Documents/github/LakeSense log --oneline -10)",
      "Bash(git commit -m 'Reset workspace for final master prompt v2 — v1 artifacts cleared \\(recoverable in history\\) *)"
    ]
  }
}
